
starting from core

replace <getrandom> (buf, buffen, _) by
    for i<64> in 0 to buffen -1 do
        @[buf + i] := nondet as urandom
    end
    return buffen
end



secret global sk
public global pk

halt at <exit>

explore all
